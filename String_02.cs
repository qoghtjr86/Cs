using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class String_02 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        string text;
        text = "HelloWorld";
        print(text);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
